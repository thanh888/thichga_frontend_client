// Interface for the SupportContactInterface document
export interface SupportContactInterface {
  // Phone number for support
  phone?: string;

  // Telegram handle or link
  telegram?: string;

  // Messenger handle or link
  messenger?: string;

  // Zalo handle or link
  zalo?: string;

  // Facebook handle or link
  facebook?: string;
}
